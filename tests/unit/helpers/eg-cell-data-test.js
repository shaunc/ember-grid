import { egCellData } from '../../../helpers/eg-cell-data';
import { module, test } from 'qunit';

module('Unit | Helper | eg cell data');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = egCellData(42);
  assert.ok(result);
});
