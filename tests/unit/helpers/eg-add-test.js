import { egAdd } from '../../../helpers/eg-add';
import { module, test } from 'qunit';

module('Unit | Helper | eg add');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = egAdd(42);
  assert.ok(result);
});
